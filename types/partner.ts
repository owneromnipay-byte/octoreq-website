export interface PartnerBenefit {
  title: string;
  description: string;
  icon: string;
}

export interface PartnerFAQ {
  question: string;
  answer: string;
}

export interface PartnerProgram {
  title: string;
  subtitle: string;
  description: string;
  applyUrl: string;
}